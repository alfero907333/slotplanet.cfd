---
import Layout from "../../../../layouts/Layout.astro";

const { eventSlug, eventId } = Astro.params;
const API = import.meta.env.API;
const SITE_NAME = import.meta.env.SITE_NAME;

let data;

// Fetch event data
try {
  const res = await fetch(
    `https://spanelv2.andrhino.com/api/v2/schedule/${eventId}`,
  );

  if (res.status === 404) {
    // Trigger Astro 404 page
    throw new Response("Event not found", { status: 404 });
  }

  if (!res.ok) {
    throw new Response(`API Error: ${res.status}`, { status: 500 });
  }

  data = await res.json();

  if (!data.id) {
    return new Response(null, { status: 404, statusText: "Not Found" });
  }
} catch (err: any) {
  // Catch network errors or thrown Responses
  if (err instanceof Response) throw err;
  throw new Response(err.message, { status: 500 });
}

// return new Response(null, { status: 404, statusText: 'Not Found' });

const {
  date_time,
  league,
  strHomeTeam,
  strAwayTeam,
  title,
  frames,
  teams,
  event_date_time,
} = data;

// Handle missing names
const displayHome = strHomeTeam || (teams?.includes(' vs ') ? teams.split(' vs ')[0] : (teams?.includes(' VS ') ? teams.split(' VS ')[0] : teams));
const displayAway = strAwayTeam || (teams?.includes(' vs ') ? teams.split(' vs ')[1] : (teams?.includes(' VS ') ? teams.split(' VS ')[1] : ''));

// Basic translations for time, date, and sports terms
const translateSportsData = (str: string) => {
  if (!str) return str;
  
  let translated = str;
  
  // Keep everything in English
  return translated;
};

const cleanTeams = translateSportsData(teams);
const cleanLeague = translateSportsData(league);

let pageTitle = `${cleanTeams} Live Stream | Soccer100`;
if (pageTitle.length > 60) {
  pageTitle = `${cleanTeams} Live | Soccer100`.slice(0, 60);
}

let pageDesc = `Stream ${cleanTeams} live on Soccer100. Get free soccer streams and reliable totalsportek links in HD quality for the ${cleanLeague} match.`;
if (pageDesc.length > 160) {
  pageDesc = `Watch ${cleanTeams} live on Soccer100. Premium soccer streams and totalsportek links in HD quality. Free online.`.slice(0, 160);
}

export const prerender = false;
---

<Layout
  title={pageTitle}
  description={pageDesc}
  keywords={`soccer100, soccer streams, totalsportek, ${cleanTeams} live, ${cleanLeague} stream`}
>
  <!-- Full Left Ad -->
  <div class="fixed left-2 top-1/2 -translate-y-1/2 z-50 hidden xl:block">
    <script is:inline>
      atOptions = {
        'key' : '6a5a027902a4642e7f792ba4488f97cd',
        'format' : 'iframe',
        'height' : 600,
        'width' : 160,
        'params' : {}
      };
    </script>
    <script is:inline src="https://www.highperformanceformat.com/6a5a027902a4642e7f792ba4488f97cd/invoke.js"></script>
  </div>

  <div class="pt-10 px-4 max-w-3xl mx-auto text-left">
    <div
      class="space-y-6 text-center relative p-6 sm:p-8 rounded-2xl theme-card border border-white/10 shadow-2xl backdrop-blur-md"
    >
      <!-- Page Title -->
      <div
        class="text-2xl sm:text-4xl font-black theme-text-primary animate-fadeIn wrap-break-word leading-tight"
      >
        {translateSportsData(teams)}
      </div>

      <!-- League and Date Info -->
      <div
        class="flex flex-col sm:flex-row justify-center items-center gap-3 text-sm theme-text-secondary mt-2 animate-fadeIn"
      >
        <span class="px-4 py-1.5 rounded-full bg-[#f3a833] font-black text-black shadow-md uppercase tracking-wider"
          >{translateSportsData(league)}</span
        >
        <span class="sm:ml-2 font-medium theme-card px-3 py-1.5 rounded-full border border-white/5">
          • {translateSportsData(event_date_time)} ET
        </span>
      </div>

      <div class="flex justify-center w-full mt-10">
        <a
          href="play"
          rel="nofollow"
          class="inline-block w-full! sm:w-auto px-8 py-4 bg-[#f3a833] text-black font-black rounded-xl text-xl transition-all hover:bg-[#e29722] hover:scale-105 shadow-[0_0_20px_rgba(243,168,51,0.3)]"
        >
          Watch Live Now
        </a>
      </div>

      <!-- Mobile Bottom Ad -->
      <div class="flex justify-center w-full mt-8 xl:hidden">
        <div class="theme-card p-2 rounded border border-[var(--border-color)] shadow-sm overflow-hidden">
          <script is:inline>
            atOptions = {
              'key' : '6a5a027902a4642e7f792ba4488f97cd',
              'format' : 'iframe',
              'height' : 50,
              'width' : 320,
              'params' : {}
            };
          </script>
          <script is:inline src="https://www.highperformanceformat.com/6a5a027902a4642e7f792ba4488f97cd/invoke.js"></script>
        </div>
      </div>

      <div class="prose max-w-none theme-text-primary">
        <p class="text-lg leading-relaxed mb-6">
          Catch the highly anticipated matchup between <strong>{translateSportsData(displayHome)} and {translateSportsData(displayAway)}</strong>. On <strong>Soccer100</strong>, we offer highly optimized, fast <strong>soccer streams</strong> so you can follow every pass, tackle, and goal of this <strong>{translateSportsData(league)}</strong> fixture without delay. We deliver standard <strong>totalsportek</strong> styling with vastly improved server speeds.
        </p>
        <p class="text-lg leading-relaxed mb-10">
          Prepare for kickoff with our verified selection of high-definition broadcasting mirrors. As fans worldwide search for secure channels, <strong>Soccer100</strong> acts as the ultimate hub, hosting multiple active <strong>soccer streams</strong> and redundant <strong>totalsportek</strong> mirrors so you never get interrupted during key plays.
        </p>

        <h2 class="text-3xl font-black text-[#f3a833] border-b border-[var(--border-color)] pb-3 mb-6">
          Event Details: {translateSportsData(displayHome)} vs {translateSportsData(displayAway)}
        </h2>
        <p class="mb-4 leading-relaxed">
          This crucial <strong>{translateSportsData(league)}</strong> event is set to deliver amazing football action. Rather than wrestling with slow feeds or spam-heavy portals, use <strong>Soccer100</strong> to enjoy direct, clean <strong>soccer streams</strong>. Our service works as a seamless, high-speed directory that replicates classic <strong>totalsportek</strong> lists without the frustration.
        </p>
        <p class="leading-relaxed">
          The action starts live at <strong>{translateSportsData(event_date_time)} ET</strong>. Make sure you load <strong>Soccer100</strong> early to test the active live streams. We supply reliable backup channels, enabling you to switch mirrors immediately in case of any local network latency.
        </p>

        <div class="theme-card p-5 rounded-lg my-8 border-l-4 border-[#f3a833] shadow-sm">
          <p class="font-bold leading-relaxed">
            <span class="text-[#f3a833]">⚽ Pro Tip:</span> Join the <span class="text-[#f3a833]">soccer stream</span> on <span class="text-[#f3a833]">Soccer100</span> about five minutes before kickoff to guarantee a fully synchronized, buffer-free connection.
          </p>
        </div>

        <h2 class="text-3xl font-black text-[#f3a833] border-b border-[var(--border-color)] pb-3 mb-6 mt-16">
          Key Benefits of Soccer100
        </h2>
        
        <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mb-12">
          <div class="theme-card p-6 rounded-xl shadow-lg border border-[var(--border-color)]">
            <h4 class="font-black text-lg mb-2 text-[#f3a833]">⭐ Fully Curated Links</h4>
            <p class="text-sm theme-text-secondary leading-relaxed">Every stream link undergoes active testing. We prune broken links, offering higher stability than typical totalsportek aggregators.</p>
          </div>
          
          <div class="theme-card p-6 rounded-xl shadow-lg border border-[var(--border-color)]">
            <h4 class="font-black text-lg mb-2 text-[#f3a833]">💎 Ultra HD Broadcasts</h4>
            <p class="text-sm theme-text-secondary leading-relaxed">Watch every minute of the action in high resolution. Our curated soccer streams prioritize maximum visual clarity.</p>
          </div>

          <div class="theme-card p-6 rounded-xl shadow-lg border border-[var(--border-color)]">
            <h4 class="font-black text-lg mb-2 text-[#f3a833]">📱 Multi-Device Comfort</h4>
            <p class="text-sm theme-text-secondary leading-relaxed">Enjoy full compatibility. Soccer100 delivers crisp soccer streams optimized for computers, smartphones, and tablets.</p>
          </div>

          <div class="theme-card p-6 rounded-xl shadow-lg border border-[var(--border-color)]">
            <h4 class="font-black text-lg mb-2 text-[#f3a833]">🔒 Safe Environment</h4>
            <p class="text-sm theme-text-secondary leading-relaxed">Your streaming security is vital. Soccer100 is engineered with clean, modern protocols for a safer totalsportek experience.</p>
          </div>
        </div>

        <h2 class="text-3xl font-black text-[#f3a833] border-b border-[var(--border-color)] pb-3 mb-6 mt-16">
          Frequently Asked Questions
        </h2>
        <div class="space-y-4 mb-16">
          <div class="theme-card p-6 rounded-xl shadow-md border border-[var(--border-color)] text-left">
            <h4 class="font-black text-[#f3a833] mb-2">How can I stream {translateSportsData(displayHome)} vs {translateSportsData(displayAway)} for free?</h4>
            <p class="theme-text-secondary text-sm leading-relaxed">Simply visit Soccer100, head to today's schedule, click on this event, and select any of the high-speed HD soccer streams.</p>
          </div>
          <div class="theme-card p-6 rounded-xl shadow-md border border-[var(--border-color)] text-left">
            <h4 class="font-black text-[#f3a833] mb-2">Is Soccer100 a direct totalsportek alternative?</h4>
            <p class="theme-text-secondary text-sm leading-relaxed">Yes, Soccer100 represents a modern, clutter-free upgrade, listing faster soccer streams and a vastly superior mobile interface.</p>
          </div>
          <div class="theme-card p-6 rounded-xl shadow-md border border-[var(--border-color)] text-left">
            <h4 class="font-black text-[#f3a833] mb-2">Are registration details required?</h4>
            <p class="theme-text-secondary text-sm leading-relaxed">No, all soccer streams are openly accessible. You do not need to create an account or provide payment info.</p>
          </div>
        </div>

        <div class="theme-card p-10 rounded-2xl text-center shadow-2xl mb-16 border border-white/5">
          <h2 class="text-4xl font-black mb-6">Enjoy the Match on Soccer100</h2>
          <p class="text-lg leading-relaxed max-w-2xl mx-auto opacity-90">
            Join sports fans streaming <strong>{translateSportsData(displayHome)} vs {translateSportsData(displayAway)}</strong> live. <strong>Soccer100</strong> is your home for free, high-performance <strong>soccer streams</strong> and stable <strong>totalsportek</strong> matches.
          </p>
        </div>
      </div>
    </div>
  </div>
</Layout>
